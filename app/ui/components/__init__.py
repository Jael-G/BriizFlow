'''BriizFlow UI components — the reusable pieces pages are composed from.

Each component maps to a widget the application actually uses (buttons,
toggles, fields, cards, toasts, dialogs, the sidebar shell). Style comes from
the global QSS (app.ui.theme); components only set structure, objectNames and
dynamic properties.
'''
